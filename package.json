{
  "name": "qli-buildlog-summary",
  "version": "1.0.0",
  "private": true,
  "description": "Summarize QLI build logs: task counts, failures, and the slowest tasks.",
  "license": "BSD-3-Clause",
  "bin": {
    "qli-buildlog-summary": "dist/src/cli.js"
  },
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "tsc && node --test \"dist/test/*.test.js\"",
    "docs": "typedoc"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typedoc": "^0.28.0",
    "typedoc-plugin-markdown": "^4.9.0",
    "typescript": "^5.9.0"
  }
}
