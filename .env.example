# Output format for the command-line report.
# Type: text or json. Optional; defaults to text when unset. Empty is invalid.
# Safe example: text. No credentials are needed by this program.
# To load this file: node --env-file=.env.example dist/src/cli.js examples/sample-build.log
SUMMARY_FORMAT=text
