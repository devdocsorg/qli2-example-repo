# The skeleton defines no environment variables.
# Add settings here when the project needs them; keep values safe to publish.
# For every setting, document its purpose, type, whether it is required,
# what happens when it is unset, and a safe example value.
# Document how the project loads these settings; this file does not load itself.
